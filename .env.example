# ═══════════════════════════════════════════════════════════
# OCP Environment Configuration
# Copy this to .env and fill in your values
# Or run: bash setup.sh (generates everything automatically)
# ═══════════════════════════════════════════════════════════

# ── Database ──
# Generated automatically by setup.sh or docker-compose
DB_PASSWORD=change_me_in_production

# ── Bot Security (REQUIRED) ──
# Generate: openssl rand -hex 16
BOT_ADMIN_KEY=change_me_in_production
EVOLUTION_WEBHOOK_SECRET=change_me_in_production

# ── Evolution GO ──
EVOLUTION_API_KEY=your_evolution_global_api_key
EVOLUTION_INSTANCE=OCP
EVOLUTION_INSTANCE_TOKEN=your_instance_token_uuid

# ── Restaurant ──
RESTAURANT_NAME=Orange Cheese Pizza
RESTAURANT_WHATSAPP_NUMBER=919876543210
DELIVERY_FEE=0

# ── CORS (required for production) ──
# Comma-separated origins: https://your-app.vercel.app,http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000

# ── Frontend ──
# Leave empty for Docker (nginx proxies to bot)
# Set to your backend URL for Vercel/external deployment
VITE_API_BASE_URL=
